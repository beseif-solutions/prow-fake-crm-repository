{
  "type": "credentials",
  "id": "app-credentials",
  "tests": [
    {
      "description": "should validate credentials (from .env)",
      "options": {
        "read": true
      },
      "result": "success"
    },
    {
      "description": "should fail with invalid credentials",
      "options": {
        "fields": {
          "id": 9999,
          "token": "fake-token"
        }
      },
      "result": "fail"
    }
  ]
}