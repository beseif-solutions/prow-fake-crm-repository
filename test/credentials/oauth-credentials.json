{
  "type": "credentials",
  "id": "oauth-credentials",
  "tests": [
    {
      "description": "should validate credentials (fresh oauth login)",
      "options": {
        "write": true
      },
      "result": "success"
    },
    {
      "description": "should validate credentials (from .env)",
      "options": {
        "read": true
      },
      "result": "success"
    }
  ]
}